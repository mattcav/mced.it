<section class="m-persone">
  <ul class="m-persone__list">
    {{#each tags}}
      {{#is tag "persone"}}
        {{#withSort pages "data.order"}}
            <li class="m-persone__item">
              <a class="m-persone__link" href="./{{basename}}.html">
                {{data.title}}
              </a>
              <p class="m-persone__desc">
                {{data.desc}}
              </p>
            </li>
        {{/withSort}}
      {{/is}}
    {{/each}}
  </ul>
</section>
